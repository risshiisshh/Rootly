version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: rootly:local
    container_name: rootly_app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
